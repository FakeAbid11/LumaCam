package com.lumacam.core.common

/**
 * Shared, dependency-free constants for the LumaCam modules.
 * Domain enums and dispatchers will live here as features are built (PRD §6).
 */
object LumaBuildInfo {
    const val APP_ID = "com.lumacam"
    const val MODULE = "core-common"
}
